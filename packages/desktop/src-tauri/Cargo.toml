[package]
name = "stowwit"
version = "0.1.0"
description = "Desktop shell for stowwit — semantic Git stash manager"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[[bin]]
name = "stowwit"
path = "src/main.rs"
